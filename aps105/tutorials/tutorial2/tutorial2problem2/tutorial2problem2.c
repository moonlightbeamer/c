//write a single C statement that generates a random number in the range of [-150,150] and uses it to declare and initialize variable randomChoice

#include <stdio.h>

int main(void){
    //find number of possible outcomes
    rand()%151; //finish
    return 0;
}