#include <stdio.h>

int main() {
  /*
  //write a C program that counts the number of digits in a number
  //assume 0 has 0 digits, will not count standalone 0's

  int num;
  int digitCount = 0;

  printf("Enter num: ");
  scanf("%d", &num);
  while (num!=0) {
      num = num/10;
      digitCount++;
  }
  printf("Digits = %d\n", digitCount);

  //print 15 stars using while loop
  int count = 1;
  while (count<=15) {
      printf("*");
  }


  //print 15 stars using for loop
  //starts count = 0
  for(int count = 0; count < 15; count++){
      printf("*");
  }

  //starts count = 1
  for(int count = 1; count <= 15; count++){
      printf("*");
  }
  

  // ommiting initialization & increment
  int n = -2;
  for (;n < 1;) {
    n++;  // put increment inside for loop
  }

  
  for (int n = 1; ;n++){

  }
 */

for (int m = 1, char l = 'a'; l < 'z' || m < 10; l++, m--) {

}

  return 0;
}